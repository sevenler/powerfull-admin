define('node_modules/promise/polyfill', function(require, exports, module) {

  // not "use strict" so we can declare global "Promise"
  
  var asap = require('node_modules/asap/browser-asap');
  
  if (typeof Promise === 'undefined') {
    Promise = require('node_modules/promise/lib/core')
    require('node_modules/promise/lib/es6-extensions')
  }
  
  require('node_modules/promise/polyfill-done');
  

});
