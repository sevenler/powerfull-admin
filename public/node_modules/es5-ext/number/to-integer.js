define('node_modules/es5-ext/number/to-integer', function(require, exports, module) {

  "use strict";
  
  var sign  = require("node_modules/es5-ext/math/sign/index")
    , abs   = Math.abs
    , floor = Math.floor;
  
  module.exports = function (value) {
  	if (isNaN(value)) return 0;
  	value = Number(value);
  	if (value === 0 || !isFinite(value)) return value;
  	return sign(value) * floor(abs(value));
  };
  

});
